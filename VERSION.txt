VERSION 13 — 12 septembre 2026

CORRECTION MAJEURE DES EMOTES :

Twitch a change son systeme : les nouvelles emotes ont des identifiants
"emotesv2_..." (plus des numeros). Mon code utilisait l'ancienne URL,
qui ne marchait que pour les vieux numeros. C'est pour ca que tes emotes
ne s'affichaient pas (ex. x7gionABSOLUTE).

Corrige : les emotes "emotesv2_..." utilisent maintenant la bonne URL
(emoticons/v2/...), donc elles s'affichent en images.

A confirmer : ecris une emote Twitch dans ton chat -> elle doit s'afficher
en image (plus en texte).

Rappel des choses deja OK :
- Question du debat agrandie (2 lignes) ✅
- Alertes testables avec tester-alertes.bat ✅
- Sondage /poll + timer + votes ✅
- Badges : a faire plus tard (pas encore)

Si ce fichier affiche "VERSION 13", tu as la derniere version.
Ensuite : relance demarrer-pont.bat, puis dans OBS : clic droit sur la source
-> "Actualiser".
