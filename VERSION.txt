VERSION 9 — 12 septembre 2026

CORRECTION IMPORTANTE DU CHAT :
- Le TEXTE de tes messages s'affiche enfin (avant : seul le pseudo apparaissait)
- Les BADGES (sub, mod, etc.) s'affichent aussi
- Les commandes !vote fonctionnent avec les bons pseudos

Diagnostic ajoute pour /poll :
- La fenetre noire affiche maintenant ce qu'elle recoit de Twitch
  (ex. "SONDAGE DETECTE" ou "HTTP 401 = mauvais token")

Si tu lances /poll sur Twitch et que rien n'apparait, regarde la fenetre noire
et copie-moi ce qu'elle ecrit a propos de [twitch-poll].

Si ce fichier affiche "VERSION 9", tu as la derniere version.
Ensuite : relance demarrer-pont.bat, puis dans OBS : clic droit sur la source
-> "Actualiser".
