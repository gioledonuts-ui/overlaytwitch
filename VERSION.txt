VERSION 16 — 12 septembre 2026

DIAGNOSTIC COMPLET DU TOKEN :

J'ai ajoute une verification du token au demarrage. Relance demarrer-pont.bat
et regarde les lignes "[auth] ..." en bas de la fenetre noire. Elles vont te
dire EXACTEMENT ce qui ne va pas :

- "[auth] token VALIDE — compte : 7gionny — droits : ..."  -> le token est bon
  (et tu verras s'il MANQUE un droit, avec un ⚠️)

- "[auth] token INVALIDE (HTTP 401)"  -> le token est faux/expire
  -> il faut le regenerer

- "[auth] aucun token POLL_OAUTH"  -> le champ est vide dans secrets.json

- "[auth] GET /users → HTTP 401/403 ..."  -> le token ne correspond pas au
  Client ID, ou le droit est manquant

COPIE-MOI ces lignes "[auth] ..." et je te dirai precisement quoi corriger.

Si ce fichier affiche "VERSION 16", tu as la derniere version.
